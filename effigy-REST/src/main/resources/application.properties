# Server Configuration
# Default port for local development (HTTP)
server.port=9090

# Database Configuration (commented out for testing)
#spring.datasource.url=jdbc:mysql://localhost:3306/effigy?useSSL=false&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration (commented out for testing)
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Disable DataSource auto-configuration temporarily
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# HTTPS Configuration (disabled by default for local development)
# Enabled automatically in production profile
# server.ssl.enabled=true
# server.ssl.key-store=deployment/keystore.p12
# server.ssl.key-store-password=changeit
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=tomcat
# server.ssl.enabled-protocols=TLSv1.2,TLSv1.3